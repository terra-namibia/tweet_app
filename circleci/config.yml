version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    machine:
      node:
        version: 10.3.0
      post:
        - npm install -g npm@6
      timezone: Asia/Tokyo
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.
      - run:
          name: Print the Current Time
          command: date
